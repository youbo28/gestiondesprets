package com.ayoub.gestiondespret.modelView;

/*
cette classe est utilisée dans l'annotation jsonview pour éviter la
 boucle infinie lors de l'utilisation de relations manyToOne and oneToMany 
*/
public class EmployeView {
	public static class Summary {
	}

}
